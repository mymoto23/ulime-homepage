import React from 'react';
import PreRegister from "../components/PreRegister";

const PreRegisterPage = ({history}) => {
  return(
    <PreRegister
      history={history}
    />
  );
};

export default PreRegisterPage;
